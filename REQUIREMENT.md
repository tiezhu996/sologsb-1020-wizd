# sologsb-1020 · 档案元数据核对台

> 端口：宿主 `10020` → 容器 `80`

## 原始需求

用Qwik+TypeScript+Qwik UI从0做一个档案元数据核对台。用户可以导入两组口述史或手稿记录，按标题、日期、人物、地点和编号做匹配，再对可疑匹配逐条确认。合并两条记录时，每个冲突字段都要显示来源并让用户选择保留哪一个，完成后还要能追溯哪些内容被合并、改过或被忽略。需要支持键盘密集型审核、批量操作、撤销重做和本地离线保存，界面不能因为记录量大而卡住。
